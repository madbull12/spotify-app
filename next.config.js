/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: ['i.scdn.co','platform-lookaside.fbsbx.com'],
  },
  nextConfig
}
